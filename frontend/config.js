// Point this at your deployed Worker URL (from `wrangler deploy`).
// For local dev against `wrangler dev`, use http://localhost:8787
window.API_BASE_URL = "https://hikma-business-os-api.YOUR-SUBDOMAIN.workers.dev";
